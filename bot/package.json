{
  "name": "play-scanner-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Alerts-only intraday setup scanner. Scans the US universe via Polygon, detects rule-based setups (VWAP reclaim, EMA cross + volume, candlestick, relative-volume catalyst), and texts entry/SL/TP via Twilio. NOT financial advice; never places trades.",
  "scripts": {
    "start": "node src/index.js",
    "scan:once": "node src/index.js --once",
    "backtest": "node src/backtest-run.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "luxon": "^3.5.0",
    "node-cron": "^3.0.3",
    "twilio": "^5.3.0"
  }
}
